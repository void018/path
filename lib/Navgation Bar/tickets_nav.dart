import 'package:flutter/material.dart';

class TicketsNav extends StatelessWidget {
  const TicketsNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Tickets Screen')));
  }
}
