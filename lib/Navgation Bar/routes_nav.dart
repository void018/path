import 'package:flutter/material.dart';

class RoutesNav extends StatelessWidget {
  const RoutesNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Routes Screen')));
  }
}
